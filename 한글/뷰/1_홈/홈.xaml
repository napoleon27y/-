<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:한글.뷰모델"
    x:DataType="vm:바인딩테스트"
    x:Class="한글.뷰.홈">

    <ContentPage.BindingContext>
        <vm:바인딩테스트/>
    </ContentPage.BindingContext>

    <StackLayout Margin="20">
        <Label Text="홈"/>
        <Label Text="{Binding 이름}"/>
        <Button Text="이름바꾸기" Command="{Binding 이름바꾸기}"/>
    </StackLayout>

</ContentPage>
